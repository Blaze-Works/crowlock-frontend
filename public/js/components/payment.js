import { D, SVG, Notice, truncate, log, Input, Button, wait, fetchWithTimeout, handleError } from '../utils.js';
import Screen from './ui/Screen.js';

export default class Payment extends Screen {

}
